'use strict';
let img = document.querySelector('.img-gold');

function phones(phone) {
  img.src = phone;
}
let container = document.querySelector('.container');
console.log(container);
function colors(color) {
  container.style.background = color;
}
